---
status: pending
---

# About page: use "Inventory" name and show version number

The About modal currently says "LAP Inventory". It should match the main
application name, which is just "Inventory". Additionally, the About page
should display the current application version number (from `GET /api/admin/env`
or the build-time version).
