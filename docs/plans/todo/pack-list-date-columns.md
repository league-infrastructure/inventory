# Pack list should show created and last updated dates

## Summary

The list of packs (as returned by `list_packs` and displayed in the UI)
should include a "Created" column and a "Last Updated" column so users
can see when packs were added and when they were last modified.
