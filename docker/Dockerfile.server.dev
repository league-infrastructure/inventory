# Development server with hot-reload
FROM node:20-alpine
WORKDIR /app
COPY server/package*.json ./
RUN npm ci
COPY server/ ./
RUN npx prisma generate
COPY docker/dev-server-start.sh ./dev-server-start.sh
RUN chmod +x ./dev-server-start.sh
EXPOSE 3000
CMD ["./dev-server-start.sh"]
